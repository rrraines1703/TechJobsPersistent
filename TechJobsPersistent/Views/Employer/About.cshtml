@model TechJobsPersistent.ViewModels.AddEmployerViewModel

<h1>@Model.Name</h1>

<table class="table">
    <tr>
        <th>Name: </th>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <th>Location</th>
        <td>@Model.Location</td>
    </tr>
</table>
